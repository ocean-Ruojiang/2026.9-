# 移植与验证记录

日期：2026-09-12。

## 内容来源与改动

- 从工作空间复制完整的 interference_simulator_core.py 与 interference_simulator_gui.py。
- 保留核心 HTTP 接口、计时、误差、清除和布局规则。
- GUI 补上传入核心的 random_seed；原先界面种子只用于布源，现在也能复现会话测向误差。
- 新增不依赖固定路径的启动器、无界面 CLI、标准源配置、协议演示、可安装项目和测试。
- 不包含用户目录、官方账号、令牌、历史日志或第三问策略依赖。

## 已运行检查

环境为 Windows x64、Python 3.12.14、Tk 8.6。

| 验证 | 结果 |
|---|---|
| HTTP 四接口、请求重放不重复计时 | 通过 |
| 固定位置误差与会话重置可复现 | 通过 |
| 未知 position 字段拒绝、现实超时不推进虚拟时间 | 通过 |
| 定向覆盖内外反馈 | 通过 |
| 第三问源数量校验、默认真值访问限制 | 通过 |
| 核心集成测试 | 5/5 通过 |
| 目录搬迁后使用 -I -S 启动真实CLI，示例客户端四接口、事件文件 | 1/1 通过 |
| wheel 构建安装及安装后的命令行入口 | 通过 |
| GUI 随机全向/混合布源、配置导入导出、Canvas 绘制 | 通过 |
| GUI 启动 HTTP 服务、执行进入/测向/清除/退出、轨迹与复位 | 通过 |
| GUI 关闭 site-packages（-I -S）验证 | 通过，无第三方运行依赖 |

GUI 第一次验证的断言误将 /exit 计入位置轨迹；核心只记录 enter、measure、clear 三个位置动作。断言已修正并重跑通过，没有为通过测试改变模拟器行为。

跨平台入口通过相对脚本位置定位代码；Linux/macOS 尚未实机运行，不将其标记为已验证。GUI 需要桌面、Tcl/Tk 和可用中文字体；无桌面时可运行 HTTP CLI。
